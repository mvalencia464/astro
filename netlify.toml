[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Cache AVIF files between builds to avoid reconversion
[[caches.avif_images]]
  paths = ["src/assets/**/*.avif", "public/**/*.avif"]
